list(
    APPEND INCLUDE_DIRS
    "${CMAKE_CURRENT_SOURCE_DIR}/dhcpserver"
)

list(
    APPEND SOURCES
    "${CMAKE_CURRENT_SOURCE_DIR}/dhcpserver/dhcpserver.c"
)

set(SOURCES "${SOURCES}" PARENT_SCOPE)
set(INCLUDE_DIRS "${INCLUDE_DIRS}" PARENT_SCOPE)